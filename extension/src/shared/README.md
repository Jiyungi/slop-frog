# Shared Extension Contracts

Shared TypeScript contracts, thresholds, and fixtures live here.

`fixtures.json` is shared by the extension and local detector so both sides can verify the same post shapes before integration.
